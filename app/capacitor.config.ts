import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'rating.miguelmo.dev',
  appName: 'rating',
  webDir: 'dist/rating',
  bundledWebRuntime: false
};

export default config;
